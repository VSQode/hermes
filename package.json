{
  "name": "hermes",
  "version": "0.2.0",
  "description": "Mid-stream and idle agent message injection for VS Code Copilot Chat",
  "engines": { "vscode": "^1.96.0" },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^16.x",
    "@types/vscode": "^1.80.0",
    "@vscode/vsce": "^2.22.0",
    "typescript": "^5.3.0"
  }
}